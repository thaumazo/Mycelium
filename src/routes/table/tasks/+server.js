import { GET, POST, PATCH } from '$lib/apiUtils.js';

export const GET = async (event) => {
    return GET(event);
};

export const POST = async (event) => {
    return POST(event);
};

export const PATCH = async (event) => {
    return POST(event);
};